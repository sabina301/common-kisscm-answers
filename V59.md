## Решение вариант 59 
1.
```bash
grep "^'$1'" /etc/passwd | awk -F":" '{print $3}'
``` 
2.
 ```bash
git branch barterer
git branch slick
git checkout -b enclosure
git commit zodiac
git checkout master
git commit strudel
git merge enclosure
git checkout enclosure
git merge master
git checkout master
git commit unstable
git checkout enclosure
git merge master
git checkout master
git commit never
git branch revival
git commit sierra
```

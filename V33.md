## Решение вариант 33 
 1. 
 ```bash
``` 
 2.
```bash
git commit -m "flint"
git branch disagree
git commit -m "garment"
git branch tragedy
git commit -m "sedate"
git branch octane
git checkout tragedy
git commit -m "dinner"
git commit -m "rebate"
git checkout master
git merge tragedy
git commit -m "cinema"
``` 